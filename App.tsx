import React, { useRef, useState } from 'react';
import { View, FlatList,  Text, Image, TouchableWithoutFeedback, StyleSheet } from 'react-native';
import ReelsPlayer from './src/Videos';

const App = ()=>{
  return(
   <ReelsPlayer/>
  )
}

export default App